---
const { title } = Astro.props;
---

<html lang="en">
  <head>
    <title>{title}</title>
    <meta charset="UTF-8" />
    <!-- Tailwind CSS is automatically included via Astro's integration -->
  </head>
  <body class="bg-gray-100 text-gray-900">
    <header class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold text-gray-900">{title}</h1>
        <nav class="mt-4">
          <a href="/" class="text-blue-600 hover:text-blue-800 mr-4">Home</a>
          <a href="/about/" class="text-blue-600 hover:text-blue-800 mr-4"
            >About</a
          >
          <a href="/tips/" class="text-blue-600 hover:text-blue-800">Tips</a>
        </nav>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <slot />
    </main>

    <footer class="bg-white">
      <div
        class="max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8 text-center text-gray-500"
      >
        © {new Date().getFullYear()} Pico-Breaks App. All rights reserved.
      </div>
    </footer>
  </body>
</html>
